import { Meta, Controls, Story } from '@storybook/blocks';
import * as BoardStories from "./Board.stories"

<Meta of={ BoardStories } title="Board docs" />

# Definition

Board is a components that contains Cell components and passes a onClick funtion to Cell.

## Usage

The component comes in different variants such as `5x5`, `10x10`, `15x15`.

## Inputs

Board has the following properties:
<Controls/>
