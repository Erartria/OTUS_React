import { Meta, Controls, Story } from '@storybook/blocks';
import * as CellStories from "./Cell.stories"

<Meta of={ CellStories } title="Cell docs" />

# Definition

Cell is a nonclickable element.

## Usage

The component comes in different variants such as `alive`, `dead`, `born`.

## Inputs

Cell has the following properties:
<Controls/>
